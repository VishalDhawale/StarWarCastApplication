<?xml version="1.0" encoding="utf-8"?>

<!--
    Duration = 1 means that one rotation will be done in 1 second. leave it.
    If you want to speed up the rotation, increase duration value. 
    in example 1080 shows three times faster revolution. 
    make the value multiply of 360, or the ring animates clunky 
-->
<rotate xmlns:android="http://schemas.android.com/apk/res/android"
    android:duration="1080"
    android:fromDegrees="0"
    android:pivotX="50%"
    android:pivotY="50%"
    android:toDegrees="360" >

    <shape
        android:innerRadiusRatio="3"
        android:shape="ring"
        android:thicknessRatio="8"
        android:useLevel="false" >
        <size
            android:height="@dimen/b_height_44"
            android:width="@dimen/b_height_44" />

        <gradient
            android:centerColor="@color/dgrey"
            android:centerY="0.50"
            android:endColor="@color/dgrey"
            android:startColor="@color/lgrey"
            android:type="sweep"
            android:useLevel="false" />
    </shape>

</rotate>